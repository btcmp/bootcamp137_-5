<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security" 
xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="http://www.springframework.org/schema/beans 
    http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
        http://www.springframework.org/schema/security 
        http://www.springframework.org/schema/security/spring-security-4.0.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.2.xsd">
    
    <context:component-scan base-package="com.miniproject.pos"></context:component-scan>
    
    <http auto-config="true" use-expressions="true">
		<intercept-url pattern="/items/**" access="hasAnyAuthority('ADMIN','BACK_OFFICE')"  />
	<intercept-url pattern="/adjustment/**" access="hasAnyAuthority('ADMIN','BACK_OFFICE')"/>
	<form-login
	    login-page="/login"
	    default-target-url="/welcome" authentication-success-handler-ref="homeRedirectSuccessHandler"
		authentication-failure-url="/login?error"
		username-parameter="username"
		password-parameter="password" />
	<logout logout-success-url="/login?logout" />
	<!-- access denied page -->
	<access-denied-handler error-page="/403" />
	<csrf disabled="true"/>
	</http>
	<beans:bean id="homeRedirectSuccessHandler"
    class="com.miniproject.pos.security.HomeRedirectSuccessHandler" />
    <authentication-manager alias="authenticationManager">
		<authentication-provider>
			<jdbc-user-service data-source-ref="dataSource"
				users-by-username-query=
					"select username,password, active from pos_mst_user where username=?"
				authorities-by-username-query=
					"select 
					  u.username, 
					  r.name FROM pos_mst_user u
					inner join pos_mst_role r
					on u.role_id = r.id where u.username = ? " />
		</authentication-provider>
	</authentication-manager>
</beans:beans>